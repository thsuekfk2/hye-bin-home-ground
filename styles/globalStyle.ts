import { css } from "@emotion/react";

export const globalStyle = css`
  html {
    font-size: 16px;
  }

  body {
    font-family: "Noto Sans KR", sans-serif;
  }

  select,
  input,
  button,
  textarea {
    border: 0;
    outline: 0 !important;
  }
`;
